Graph engine vytvořen pro reprezentaci/simulaci grafu protokolu OSPF;

Konvence - public metody a data jsou psány velkými písmeny, private metody a data jsou psány malými písmeny;

Implementovat hot realoding pro funkce OSPF:
  void *dlopen(const char *filename, int flags);
  
  void *dlsym(void *handle, const char *symbol);
  
  int dlclose(void *handle);
